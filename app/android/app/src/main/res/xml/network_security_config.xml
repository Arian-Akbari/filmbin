<?xml version="1.0" encoding="utf-8"?>
<!--
  Section 8.3 — the app talks HTTPS everywhere. The two exceptions below are
  the development backend as seen from an emulator (10.0.2.2) and from a
  device on the same machine (localhost); every other host stays encrypted.

  The self-signed CA is trusted only in debug builds, so a release build can
  never accept it. Certificate pinning on top of this lives in
  lib/core/network/certificate_pinning.dart.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
