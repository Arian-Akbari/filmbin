[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -q --strict-markers -m "not live"
markers =
    live: hits the real IMDb service (deselected by default, run with -m live)
filterwarnings =
    ignore::DeprecationWarning
